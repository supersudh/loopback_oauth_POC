.container
  if user && user.username
    .row
      .col-md-12
        .well
          h1 User Logged in!
          p.small User Id: #{user.id}
          p.small Email: #{user.email}
  block content
